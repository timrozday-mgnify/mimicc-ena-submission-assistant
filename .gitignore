.venv/
__pycache__/
*.pyc
vendor/
server/static/dh/
.env
.DS_Store

# Local-dev fallback for the schema library when SCHEMAS_CONTAINER_DIR isn't
# set (see server/_bootstrap.py:schemas_dir) — production uses the /schemas
# bind mount instead.
.local/

# SQLite fallback DB (used when DATABASE_URL is unset — tests + lightweight local).
.data/
*.sqlite3

# Test/tooling artifacts
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/

# Editors
.idea/
.vscode/
